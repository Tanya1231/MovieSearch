<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieSearch" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimaryDark">@color/cornsilk</item>
        <item name="android:windowBackground">@color/cornsilk</item>
        <item name="android:colorBackground">@color/cornsilk</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>